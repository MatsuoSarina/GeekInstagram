<!DOCTYPE html>
<link rel="stylesheet" type="text/css" href="login.css">
<script src="login.js"></script>

<title>GeekInstagram</title>
<p>※登録したメールアドレスとパスワードを入力してください。</p>

<div class="frame">
  <div class="top-image">
  <div class="icon">
    
  </div>

    <h3>GeekInstagramにログイン</h3>
</div>
<br>

    <div class="input">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <%= f.label :名前 %><br />
        <%= f.text_field :name, autofocus: true, style:"border: none; background-color: #eee;" %>
    </div>

    <div class="input2">
      <%= f.label :メールアドレス %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", style:"border: none; background-color: #eee;" %>
    </div>
    
    <div class="input2">
      <%= f.label :パスワード %><br />
      <%= f.password_field :password, autocomplete: "current-password", style:"border: none; background-color: #eee;" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :今後入力を省略する %>
      </div>
    <% end %>

    <div class="actions">
      <%= f.submit "ログイン", class: "btn btn-primary" %>
    </div>

  <% end %>

  <%= render "users/shared/links" %>
</div>
